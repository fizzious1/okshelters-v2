package com.shelternav.app.ui.components

// User location pulse ring is rendered on the MapLibre layer,
// not as a Compose overlay, for performance.
// MapLibre's built-in location component handles the pulsing indicator.
